<!DOCTYPE html>
<html>
<head>
    <title>Produce - GroceryGo</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <% include("partials/header") %>

    <main class="container">

        <h2>🥕 Produce</h2>

        <!-- Item List -->
        <div id="aisleItems" class="aisle-grid"></div>

    </main>

    <script>
        async function loadAisle() {
            const res = await fetch("/api/aisle/produce");
            const items = await res.json();

            const container = document.getElementById("aisleItems");
            container.innerHTML = "";

            items.forEach(item => {
                container.innerHTML += `
                    <div class="item-card">
                        <h3>${item.name}</h3>
                        <p>Aisle: ${item.aisle}</p>
                        <button onclick="deleteItem(${item.id})">Delete</button>
                    </div>
                `;
            });
        }

        async function deleteItem(id) {
            await fetch("/api/groceries/" + id, { method: "DELETE" });
            loadAisle(); // refresh list
        }

        document.addEventListener("DOMContentLoaded", loadAisle);
    </script>

</body>
</html>
